upstream {{ .ClusterName }}_{{ .Namespace }}_{{ .Service }} {
    {{- range .Endpoints }}
    server {{ . }}:{{ $.ServicePort }};
    {{- end }}
}

server {
    listen {{ .IP }}:{{ .ServicePort }};
    proxy_pass {{ .ClusterName }}_{{ .Namespace }}_{{ .Service }};
}
